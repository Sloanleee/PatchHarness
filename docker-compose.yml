services:
  patchharness-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      PATCHHARNESS_LLM_PROVIDER: ${PATCHHARNESS_LLM_PROVIDER:-mock}
    volumes:
      - ./.storage:/app/.storage
